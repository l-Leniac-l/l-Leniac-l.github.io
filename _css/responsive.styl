breakpoint(n, m=null)
  if m is a 'string'
    condition = "only screen and (max-width: "+n+") and (orientation: "+m+")"
  else
    condition = o"nly screen and (min-width: "+n+")"

  @media condition
    {block}
